//search substring in files
grep -rI -e 'afephone' --exclude=\*.{c,h,mib,log,svg} / 2> /dev/null
-I skip binary
-r recursive

//matrix
cmatrix -ba
-bold
-asynchronous

//unban fail2ban
fail2ban-client set sshd unbanip 192.168.1.1

//vgs
vgs
lvs
lvextend -L+50G /dev/mapper/vg
lvextend -L+45G /dev/mapper/vg
resize2fs /dev/mapper/vg
df -h
